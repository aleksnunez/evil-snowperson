import React from 'react';
import './Header.scss';  // Import the SCSS file here

const Header = () => {
    return (
        <div className="header">  {/* Add this wrapper div */}
            <h1>
                SF Math Circle: Evil Snowperson!
            </h1>
        </div>
    )
}

export default Header;
